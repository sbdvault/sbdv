# Amvera Cloud — Next.js Docker config
# Docs: https://docs.amvera.ru/applications/configuration/docker.html

meta:
  environment: docker
  toolchain:
    name: docker

build:
  dockerfile: Dockerfile

run:
  containerPort: 3000
  persistenceMount: /data
